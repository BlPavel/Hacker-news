import { SetPointsPipe } from './set-points.pipe';

describe('SetPointsPipe', () => {
  it('create an instance', () => {
    const pipe = new SetPointsPipe();
    expect(pipe).toBeTruthy();
  });
});
