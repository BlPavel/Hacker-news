import { SetCommentsPipe } from './set-comments.pipe';

describe('SetCommentsPipe', () => {
  it('create an instance', () => {
    const pipe = new SetCommentsPipe();
    expect(pipe).toBeTruthy();
  });
});
